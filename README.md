# Mero Chrome Wallet

First run ```yarn install``` to install the libraries.

For active development run ```yarn run watch```

To build for production run ```yarn run build```

To create the .zip required to post to the Chrome Web Store run ```yarn run build-zip```